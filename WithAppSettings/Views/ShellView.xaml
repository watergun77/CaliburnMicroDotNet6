<Window x:Class="WithAppSettings.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WithAppSettings.Views"
        mc:Ignorable="d"
        Title="ShellView" Height="100" Width="400">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock MinWidth="100" Text="First Name"/>
            <TextBox MinWidth="100" Text="{Binding FirstName}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock MinWidth="100" Text="Last Name"/>
            <TextBox MinWidth="100" Text="{Binding LastName}"/>
        </StackPanel>
    </StackPanel>
</Window>
